{
  "name": "period-notif-typescript",
  "version": "1.0.0",
  "description": "typescript version of the other garbage one",
  "scripts": {
    "watch": "tsc -w",
    "build": "tsc",
    "dev": "concurrently --kill-others \"yarn watch\" \"nodemon --inspect dist/src/index.js\""
  },
  "author": "Me",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.7.10",
    "concurrently": "^6.2.1",
    "nodemon": "^2.0.12",
    "typescript": "^4.4.2"
  },
  "dependencies": {
    "discord.js": "^13.1.0"
  }
}
